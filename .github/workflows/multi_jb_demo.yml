name: Multi-Job Demo

on: push

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Linting code..."

  test:
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - run: echo "Running tests after linting..."
